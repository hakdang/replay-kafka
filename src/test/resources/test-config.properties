# Test configuration for Kafka Replayer
source.bootstrap.servers=localhost:9092
target.bootstrap.servers=localhost:9092
source.topic=test-source-topic
target.topic=test-target-topic
consumer.group.id=test-group
consumer.timeout=10
processing.delay=0
